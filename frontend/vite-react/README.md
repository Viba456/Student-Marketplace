# Vite React Frontend

Dev setup:

```bash
cd frontend/vite-react
npm install
npm run dev
# open http://127.0.0.1:3001
```

The dev server proxies `/auth` requests to `http://127.0.0.1:8000` (see `vite.config.js`).
